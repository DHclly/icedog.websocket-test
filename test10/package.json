{
  "name": "websocket-test10",
  "description": "test the websocket connection count",
  "private": true,
  "scripts": {
    "dev": "node ./server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.16.0"
  }
}
